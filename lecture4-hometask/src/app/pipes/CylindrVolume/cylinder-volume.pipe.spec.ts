import { CylinderVolumePipe } from './cylinder-volume.pipe';

describe('CylinderVolumePipe', () => {
  it('create an instance', () => {
    const pipe = new CylinderVolumePipe();
    expect(pipe).toBeTruthy();
  });
});
