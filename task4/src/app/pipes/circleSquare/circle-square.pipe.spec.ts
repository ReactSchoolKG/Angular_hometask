import { CircleSquarePipe } from './circle-square.pipe';

describe('CircleSquarePipe', () => {
  it('create an instance', () => {
    const pipe = new CircleSquarePipe();
    expect(pipe).toBeTruthy();
  });
});
